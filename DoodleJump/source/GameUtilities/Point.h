#pragma once
/**
 * \Point
 * Basic gameplay elements:
 * Will be used for the following items:
 * 1)
 */
struct PointF {
	float x, y;
    PointF(float initX, float initY)
	: x(initX),
	  y(initY) {}
    
};